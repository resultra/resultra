

function loadCaptionIntoHeader(headerElem, recordRef) {
	// no-op
}

function initCaptionRecordEditBehavior($captionContainer,componentContext,captionObjectRef) {	
	$headerContainer.data("viewFormConfig", {
		loadRecord: loadRecordIntoHeader
	})
	
}
